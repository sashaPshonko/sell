# cp .env.example .env
# Токен: DevTools → Application → Cookies → playerok.com → token (только JWT)
PLAYEROK_TOKEN=paste_jwt_here

# Cookies (auid) для chatMessages — один раз на машине:
# открой чат на playerok → Network → chatMessages → Copy as cURL → paste.curl → npm run capture-curl
# (создаст captures/session.cookie, в git не попадает)
