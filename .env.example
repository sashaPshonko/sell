# cp .env.example .env
# Токен: DevTools → Application → Cookies → playerok.com → token (только JWT)
PLAYEROK_TOKEN=paste_jwt_here

# Cookies (auid) для chatMessages → captures/session.cookie (не в git):
#   chatMessages → Copy as cURL → captures/paste.curl → npm run set-session -- captures/paste.curl
# На VPS с Mac: scp captures/session.cookie root@IP:~/sell/captures/
